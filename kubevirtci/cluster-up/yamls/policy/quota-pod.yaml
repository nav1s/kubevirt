apiVersion: v1
kind: Pod
metadata:
  name: quota-pod
  namespace: quota-demo
spec:
  containers:
  - name: app
    image: nginx:1.27
    resources:
      requests:
        cpu: "250m"
        memory: 256Mi
      limits:
        cpu: "500m"
        memory: 512Mi

