apiVersion: batch/v1
kind: Job
metadata:
  name: hello
spec:
  template:
    spec:
      containers:
      - name: hello
        image: busybox:1.36
        command: ['sh', '-c', 'echo "Hello from a Pod template!" && sleep 5']
      restartPolicy: OnFailure
