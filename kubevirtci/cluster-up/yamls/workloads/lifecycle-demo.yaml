apiVersion: v1
kind: Pod
metadata:
  name: lifecycle-demo
spec:
  containers:
  - name: demo
    image: busybox:1.36
    command: ['sh', '-c', 'echo "Running!" && sleep 10 && echo "Done!"']
  restartPolicy: Never
